export interface SelectSortStep {
  array: number[];
  startIndex: number;
  currentIndex: number;
  minimumIndex: number;
  iterations:number;
  steps:number;
  sorted: boolean;
  initial: boolean;
}
